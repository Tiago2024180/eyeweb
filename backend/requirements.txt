# ===========================================
# Eye Web Backend — Dependências Python (FastAPI)
# ===========================================
# Instalar com: pip install -r requirements.txt

# --- Framework Web ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# --- Manipulação de Dados ---
pandas>=2.0.0
pyarrow>=14.0.0

# --- HTTP Requests ---
# Para aceder aos ficheiros Parquet no Hugging Face
requests>=2.31.0
httpx>=0.26.0
aiohttp>=3.9.0

# --- Hugging Face Hub ---
huggingface_hub>=0.20.0

# --- Cache ---
# Cache LRU avançado com TTL
cachetools>=5.3.0

# --- Variáveis de Ambiente ---
python-dotenv>=1.0.0

# --- CORS ---
# Já incluído no FastAPI, mas mencionado para clareza

# --- Validação ---
pydantic>=2.5.0
pydantic-settings>=2.1.0

# --- Logging ---
# structlog>=24.1.0  # Opcional: logging estruturado

# ===========================================
# URL CHECKER — Novas dependências
# ===========================================

# --- Supabase (Cache de URLs) ---
supabase>=2.0.0

# --- Groq (AI com Llama 3) ---
groq>=0.4.0

# --- Async HTTP ---
# aiohttp já está incluído acima

# ===========================================
# ADMIN MFA — Autenticação de dois fatores
# ===========================================

# --- TOTP (Time-based One-Time Password) ---
pyotp>=2.9.0
aiosmtplib>=3.0.0
