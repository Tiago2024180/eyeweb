# ===========================================
# Eye Web Backend — Variáveis de Ambiente
# ===========================================
# Copia este ficheiro para .env e preenche os valores.
# NUNCA fazer commit do .env!

# Ambiente: development, staging, production
ENVIRONMENT=development

# Modo debug (mostra erros detalhados)
DEBUG=true

# Repositório do dataset no Hugging Face
HF_DATASET_REPO=Samezinho/eye-web-breaches

# Token do Hugging Face (opcional para repos públicos)
HF_TOKEN=

# Tamanho máximo do cache (número de partições)
CACHE_MAX_SIZE=100

# Tempo de vida do cache em segundos
CACHE_TTL_SECONDS=3600
